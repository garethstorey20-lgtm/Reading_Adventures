// PILOT ONLY — copy this file to drive-config.js and fill in your values.
// API key can also live in tenants.json under "_config.apiKey" (one key for all schools).

window.DRIVE_PILOT_CONFIG = {
  fileId: "",
  apiKey: "",
  tenantsJsonUrl: "tenants.json",
};
